# Supply-chain hardening: only install package versions published at least
# 14 days ago. The window is relative to "now", computed dynamically by npm
# at install time (not a hardcoded date). Requires npm >= 11.10.
min-release-age=14
